<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the content of the activities is drawn by Compose, this only sets the window background -->
    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- for activities without UI which finish immediately -->
    <style name="InvisibleTheme" parent="android:Theme.Translucent.NoTitleBar" />
</resources>
